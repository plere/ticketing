server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticketing_user
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

auth:
  private_key: sidep.jks
  password: sidep1234
  alias: sidep
